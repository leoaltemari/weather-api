export const PORT = Number(process.env.PORT) || 4000;
export const BUIENRADAR_URL = process.env.BUIENRADAR_URL || 'https://data.buienradar.nl/2.0/feed/json';
export const REQUEST_TIMEOUT_MS = Number(process.env.REQUEST_TIMEOUT_MS) || 8000;
